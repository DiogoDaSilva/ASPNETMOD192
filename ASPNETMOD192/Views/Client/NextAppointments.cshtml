@model Client

<h1>@sharedLocalizer["Next Appointments:"] </h1>

@sharedLocalizer["Client's Schedule"]: @Model.Name

<ul>
    @foreach(Appointment ap in Model.Appointments)
    {
        <li> @ap.Date - @ap.Time - Staff: @ap.Staff.Name [@ap.Staff.StaffNumber] </li>
    }
</ul>